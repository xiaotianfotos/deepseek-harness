# HomeRail-owned, unattended JSON-RPC composition. HomeRail injects one
# turn-local MCP proxy and keeps stdout reserved for the SDK protocol.

- id: llm-deepseek
  name: '@deepseek-ai/dsh-llm-deepseek'
  config:
    apiKeyEnv: DEEPSEEK_API_KEY
    streamIdleTimeoutMs: 172800000
    models:
      - id: !!js process.env.DSH_MODEL
        contextWindow: !!js Number(process.env.DSH_CONTEXT_WINDOW ?? 200000)

- id: homerail-tools
  name: '@deepseek-ai/dsh-mcp-client'
  config:
    serverName: homerail
    transport: stdio
    command: !!js process.execPath
    args:
      - !!js process.env.HOMERAIL_DSH_MCP_SCRIPT
    cwd: !!js process.env.DSH_CWD
    env:
      HOMERAIL_MCP_BRIDGE_URL: !!js process.env.HOMERAIL_MCP_BRIDGE_URL
      HOMERAIL_MCP_BRIDGE_TOKEN: !!js process.env.HOMERAIL_MCP_BRIDGE_TOKEN
    failOnStartupError: true
    reconnect:
      enabled: false

- id: agent-spine
  name: '@deepseek-ai/dsh-agent-spine-demo'
  config:
    includeHarnessIdentity: false
    includeRuntimeContext: false
    persona: !!js process.env.DSH_SYSTEM_PROMPT
    workspaceContext: false
    skills:
      enabled: false
    toolBash: false
    toolJobs: false

- id: sessions
  name: '@deepseek-ai/dsh-session-persistence-jsonl'
  config:
    root: !!js process.env.DSH_SESSION_ROOT
    compression: none

# The protocol handshake waits for Loader settlement, so MCP discovery is
# complete before the caller can send the first turn.
- id: sdk-jsonrpc-server
  name: '@deepseek-ai/dsh-sdk-jsonrpc-server'
  config:
    maxTokensAsSuccess: true
